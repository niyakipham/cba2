source "https://rubygems.org"

# Hello! This is where you manage which Jekyll version is used to run your site.
# When you run `bundle install`, RubyGems will look at this file for dependencies.
# To install the latest version of Jekyll, remove the constrain from the line below.
gem "jekyll", "~> 4.3.3" # Hoặc phiên bản ổn định mới nhất

# If you have anyplugins, add them here!
gem "jekyll-feed", "~> 0.17.0"
gem "jekyll-seo-tag", "~> 2.8.0"
gem "jekyll-sitemap", "~> 1.4.0"
gem "jekyll-sass-converter", "~> 3.0.0" # Quan trọng cho SCSS

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
# and use it as normal. Change 'x64-mingw32' to 'mingw32' if you are using a
# 32-bit version of Ruby.
# gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]

# Pagination (Nếu bạn cần phân trang)
# gem "jekyll-paginate-v2"

# Group github-pages to avoid conflicts if deploying on GitHub Pages
# group :jekyll_plugins do
#   gem "github-pages", "~> 228" # Phiên bản có thể thay đổi
# end